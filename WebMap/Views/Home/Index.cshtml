@model List<WebMap.Models.Location>

<div id="map"></div>

@section scripts {
    <script>
        // Define the initMap function in global scope
        function initMap() {
           

        
        }

        // No need to call initMap here, as it will be called by Google Maps script.
    </script>

    <!-- Load the Google Maps script AFTER the above script block -->
    <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_KEY=initMap" async defer></script>
}

@section styles {
    <style>
        #map {
            height: 400px;
            // Adjust the height as per your requirements width: 100%;
        }
    </style>
}
